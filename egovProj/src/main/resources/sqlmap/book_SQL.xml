<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace : xml파일은 여러개일 수 있음. 이를 구별하기 위한 식별 용도로 사용 -->
<mapper namespace="kr.or.ddit.mapper.BookMapper">

	<!-- 도서 목록 -->
	<select id="list" resultType="bookVO">
		SELECT BOOK_ID, TITLE, CATEGORY,
		PRICE, INSERT_DATE
		FROM BOOK
	</select>

	<select id="detail" parameterType="int" resultType="bookVO">
		select
		book_id , title , category , price , INSERT_DATE , content
		from book
		where book_id
		= #{bookID}
	</select>

	<update id="update" parameterType="bookVO">

		UPDATE book
		SET
		category=#{category}
		,price =#{price}
		WHERE
		book_id=#{bookId}
	</update>

	<insert id="insert" parameterType="bookVO">
		<selectKey keyProperty="bookId" order="BEFORE"
			resultType="int">
			select max(book_id+1) from book
		</selectKey>
		INSERT INTO book (
		book_id,
		title,
		category,
		price,
		insert_date,
		content
		) VALUES (
		#{bookId},
		#{title},
		#{category},
		#{price},
		#{insertDate},
		#{content}
		)
	</insert>
</mapper>










