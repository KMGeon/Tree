<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatiswork.mapper.MemberMapper">

    <insert id="save1" parameterType="com.example.mybatiswork.dto.MemberMapperDTO">
        insert into member(member_id,member_email, member_password, member_name)
        values(1,#{member_email}, #{member_password}, #{member_name})
    </insert>

</mapper>