spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://study-database:3306/CStudy
    username: root
    password: 1234
  redis:
    host: study-redis
    port: 6379
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

